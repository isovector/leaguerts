using System;
using System.Collections.Generic;
using System.Text;

namespace LeagueEngine {
    /// <summary>
    /// Describes how well known a point on the map is
    /// </summary>
    public enum Visibility {
        Masked = 0, Fogged = 1, Visible = 3
    }
}
